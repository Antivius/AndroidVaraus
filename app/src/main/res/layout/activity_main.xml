<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="8dp"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Spinner
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:id="@+id/esitykset"
        android:layout_below="@+id/logout"
        android:layout_toRightOf="@+id/varaus_button"
        android:layout_toEndOf="@+id/varaus_button"
        android:layout_marginTop="39dp">

    </Spinner>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/reserve_button"
        android:id="@+id/varaus_button"
        android:layout_alignTop="@+id/esitykset"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Tervetuloa"
        android:id="@+id/tervetuloa"
        android:layout_alignBottom="@+id/logout"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Logout"
        android:id="@+id/logout"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/esitykset"
        android:layout_alignEnd="@+id/esitykset" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Varaukset"
        android:id="@+id/omatVaraukset"
        android:layout_toLeftOf="@+id/logout" />

</RelativeLayout>
